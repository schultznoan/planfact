import { FlexRow } from '../../PrimaryComponents/FlexRow'
import TotalString from './components/TotalString'

const OperationBottom = () => {
  return (
    <FlexRow style={{gap: "10px", padding: "13px 0px"}}>
      <TotalString />
    </FlexRow>
  )
}

export default OperationBottom